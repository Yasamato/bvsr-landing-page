/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        images: {
            layoutRaw: true
        }
    },
    reactStrictMode: true,
}

module.exports = nextConfig
